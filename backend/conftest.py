"""
Pytest configuration.

The presence of this file at the backend/ root ensures the directory is
inserted onto sys.path, so tests can `import app...` regardless of the
directory pytest is invoked from.
"""
